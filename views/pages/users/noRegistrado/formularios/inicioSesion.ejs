<!DOCTYPE html>
<html lang="es">
<%- include("../../../../templates/headTag.ejs", {title: 'Iniciar Sesión'}); %>
<body>
  <%- include("../../../../templates/header.ejs"); %>

  <form id="formLogin" class="formulario flexColumn">
    <img src="./media/imagenes/icono.ico" alt="Logo_SPA">
    <h2>Iniciar Sesión</h2>
    
    <div class="flexColumn">
      <label for="cliente">Número de Cliente</sub></label>
      <input
        id="cliente"
        type="text"
        name="cliente"
        minlength="4"
        placeholder="Número de Cliente"
        required
      />
    </div>

    <p style="display: <%= errorLogin === 'noPerson' ? 'block' : 'none' %>; color: red;">
      El número de Cliente no está registrado.
    </p>

    
    <div class="flexColumn">
      <label for="pass">Contraseña</label>
      <input
      id="pass"
      type="password"
      name="pass"
      minlength="8"
      maxlength="20"
      placeholder="Contraseña"
      required
      />
    </div>

    <p style="display: <%= errorLogin === 'noPass' || errorLogin === 'logueado'
      ? 'block'
      : 'none' %>; color: red;"
    >
      <% if (errorLogin === 'noPass') { %>
        La contraseña es incorrecta. Por favor, intente nuevamente.
      <% } else if (errorLogin === 'logueado') { %>
        El usuario con el que intenta iniciar sesión se encuentra ya logueado.
      <% } %>
    </p>

    <div class="registro flexRow">
      <a href="/olvideMiContrasenia">¿Olvidaste tu contraseña?</a>
      <a href="/registro">REGISTRARME</a>
    </div>

    <div id="botones" style="display: flex;">
      <button type="submit">Ingresar</button>
    </div>
  </form>

  <%- include("../../../../templates/footer.ejs"); %>
  <script src="./logicas/formularios/inicioSesion.js"></script>
</body>
</html>